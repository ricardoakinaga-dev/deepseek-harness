/**
 * @deepseek-ai/dsh-resilient-compaction — an opt-in profile patch for bounded
 * compaction and pre-dispatch context admission. Runtime behavior lives in the
 * plugin inserted by `cordis.patch.yml`.
 * @module @deepseek-ai/dsh-resilient-compaction
 */

export {}
